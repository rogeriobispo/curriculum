import styled from 'styled-components';

const Container = styled.div`
  position: fixed;
  height: 100%;
  width: 450px;
  background-image: linear-gradient(to right top, #909095 40%, #f17a7b);
  background-image: linear-gradient(to right top, #3b3b40 40%, #ff3a3b);
  background-color: #3b3b30;
  color: #fff;
  z-index: -1;
`;

export default Container;
