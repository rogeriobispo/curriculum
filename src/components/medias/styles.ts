import styled from 'styled-components';

const Container = styled.div`
  position: absolute;
  top: 0px;
  margin: 0;
  padding: 0;
  left: 0px;

  ul {
    list-style: none;
    font-size: 25px;
    display: flex;
  }

  li {
    margin: 30px 60px;
  }

  a {
    color: white;
  }
`;

export default Container;
